# Locations v1 manual UI text guide

This batch avoids injecting new Persian UI labels directly into existing components.

Suggested Persian labels to edit manually later if you want:

- City help text:
  شهر را از لیست انتخاب کن تا طول/عرض جغرافیایی و timezone برای نسخه واقعی چارت آماده باشد.

- Optional import/backup labels from previous reports batch:
  Export visible TXT -> خروجی TXT از نتایج فعلی
  Export visible JSON -> خروجی JSON از نتایج فعلی
  Export all JSON -> بکاپ کامل JSON
  Import JSON -> وارد کردن JSON

For now the code keeps English action labels where they were added by safe batches.
